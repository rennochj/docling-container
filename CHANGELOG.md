# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Features

- Add GHCR example targets to Makefile
  - Add `ghcr-pull` to pull latest image from GitHub Container Registry
  - Add `ghcr-run-*` targets for all example conversions using published image
  - Add `ghcr-quick-start` for easy testing of GHCR image
  - Add `ghcr-login.sh` helper script for GHCR authentication
  - Fix `push-ghcr.sh` authentication check to detect existing credentials

## [0.2.0] - 2025-10-18

### Features

- Add automated release workflow
  - Implement local-only release process with Makefile targets
  - Add multi-platform Docker builds (amd64 + arm64)
  - Configure GHCR publishing with authentication
  - Add conventional commit changelog generation with git-cliff
  - Create comprehensive release documentation (RELEASE.md)
  - Add version management with bump2version
  - Update README with GHCR instructions and badges
  - Create release scripts: setup-buildx, build-multiplatform, push-ghcr, release

### Bug Fixes

- Configure setuptools to exclude output directory
  - Add explicit package configuration in pyproject.toml
  - Prevent setuptools from discovering output/ as a package

## [0.1.0] - 2025-10-17

### Features

- Initial release of docling-container
- Multi-format document conversion (PDF, DOCX, PPTX, HTML, Markdown, images)
- URL support for converting remote documents
- Image extraction with configurable resolution
- Batch processing with recursive directory support
- Comprehensive Makefile for common operations

### Documentation

- Complete README with usage examples
- CLAUDE.md for AI assistant guidance
- Extensive test suite with 28+ tests

<!-- generated by git-cliff -->

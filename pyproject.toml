[project]
name = "docling-forge"
version = "0.3.6"
description = "Powerful Docker-based document conversion tool built with Docling"
requires-python = ">=3.13"
dependencies = [
    "docling>=2.0.0",
    "click>=8.1.0",
    "pyyaml>=6.0.0",
    "opencv-python-headless>=4.0.0",  # Use headless version to save ~30MB
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
]

[project.scripts]
docling-convert = "docling_forge.main:cli"
docling = "docling_forge.main:cli"

[tool.setuptools]
packages = ["docling_forge"]

[tool.setuptools.package-data]
docling_forge = ["py.typed"]

[tool.uv]
package = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
filterwarnings = [
    "error",  # Turn warnings into errors for our own code
    "ignore::DeprecationWarning:docling_core",  # Ignore deprecations from docling_core
    "ignore::DeprecationWarning:pydantic",  # Ignore Pydantic deprecation warnings
    "ignore::UserWarning:pydantic",  # Ignore Pydantic user warnings
    "ignore:.*@model_validator.*:DeprecationWarning",  # Ignore model_validator deprecation warnings
]
